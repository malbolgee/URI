#include <stdio.h>

int main () {

  int i, a, b, cont = 0, contimpar = 0;
  int a1 = 0, b1 = 0;

  scanf("%d", &a);
  scanf("%d", &b);

  if (a > b){

    for (i)


  }

  
  printf("DEBUG: %d\n", contimpar);
  printf("Quantidade de impares: %d\n", cont);

}
